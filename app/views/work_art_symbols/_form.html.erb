<%= form_for(@work_art_symbol) do |f| %>
  <% if @work_art_symbol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_art_symbol.errors.count, "error") %> prohibited this work_art_symbol from being saved:</h2>

      <ul>
      <% @work_art_symbol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
