<%- model_class = Artwork -%>


<section id="author" class="bg-light-gray">
  <div class="container">
    <div class="row">
      <div class="col-lg-12 text-center">
        <h2 class="section-heading">Obras por Autor</h2>
        <h3 class="section-subheading text-muted">Lorem ipsum dolor sit amet consectetur.</h3>
      </div>
    </div>

      <% i = 0 %>
      <% @authors.each  do |author| %>
          <% if i == 0 then %>
              <div class="row">
          <% end %>
          <div class="col-md-3 portfolio-item">
            <a href="<%= author_path(author) %>">
              <div>
                <%= image_tag author.avatar.url(:thumb) %>
              </div>
            </a>
            <h4>

              <a href="<%= artworks_path(:author_show => author.id) %>"><%= author.name %></a>
            </h4>
            <p><%= author.biography %></p>
          </div>

          <% i += 1 %>
          <% if i == 4 then %>
              <% i = 0 %>
              </div>
          <% end %>

      <% end %>

  </div>
  </div>
</section>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_artwork_path(:parent_id =>params[:parent_id]),
            :class => 'btn btn-primary' %>


<% content_for :css_application do %>
    <%= stylesheet_link_tag "artworks" %>
<% end %>
